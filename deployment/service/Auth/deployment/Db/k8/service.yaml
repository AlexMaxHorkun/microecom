apiVersion: v1
kind: Service
metadata:
  name: db-auth
  labels:
    run: auth-db-service
spec:
  type: NodePort
  ports:
    - port: 5432
      protocol: TCP
      name: pgsql
  selector:
    app: auth-db